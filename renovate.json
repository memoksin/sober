{
	"$schema": "https://docs.renovatebot.com/renovate-schema.json",
	"extends": ["config:recommended", ":semanticCommits"],
	"timezone": "Europe/Istanbul",
	"schedule": ["before 9am on monday"],
	"prConcurrentLimit": 3,
	"prHourlyLimit": 0,
	"rangeStrategy": "bump",
	"lockFileMaintenance": { "enabled": true, "schedule": ["before 9am on monday"] },
	"packageRules": [
		{
			"description": "One pull request a week for everything that is not a major.",
			"matchUpdateTypes": ["minor", "patch", "pin", "digest"],
			"groupName": "weekly dependencies",
			"groupSlug": "weekly"
		},
		{
			"description": "A major is read, not skimmed, so it arrives on its own.",
			"matchUpdateTypes": ["major"],
			"dependencyDashboardApproval": true
		},
		{
			"description": "TypeScript stays on 6.x until dependency-cruiser supports 7 (ADR 0026 note, STRUCTURE.md).",
			"matchPackageNames": ["typescript"],
			"allowedVersions": "<7"
		},
		{
			"description": "GitHub Actions move together and monthly; they are not application code.",
			"matchManagers": ["github-actions"],
			"groupName": "github actions",
			"schedule": ["before 9am on the first day of the month"]
		}
	]
}
