{
	"host": "Claude Code",
	"slots": {
		"args": "$ARGUMENTS",
		"intent": "The user wants to build: **$ARGUMENTS**",
		"plan": "/sober:plan",
		"decide": "/sober:decide",
		"brief": "/sober:brief",
		"next": "/sober:next",
		"ask": "Ask with AskUserQuestion, this host's own question tool — the one question, the option labels only — and pass `decide` exactly the option they picked. For a brief or a merge, ask a yes and a no the same way and call `approve` or `accept` with `confirmed: true` only on an explicit yes. Never answer on their behalf, and never treat a reply you did not get as one.",
		"spawn-guard": "**The block reaches into this session.** This plugin installs a hook: an agent spawn whose description or prompt names a held node is denied, and the denial says which decision is unanswered. There is no way around it and no setting that turns it off — answer the decision. If you are told a spawn was denied, do not rephrase it to get past the guard; say what is held and offer `/sober:decide`."
	}
}
