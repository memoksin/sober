{
	"host": "OpenCode",
	"slots": {
		"args": "the id the user gave",
		"intent": "Read what the user asked for, in their own words, from this conversation. That is what they want built.",
		"plan": "the **plan** skill",
		"decide": "the **decide** skill",
		"brief": "the **brief** skill",
		"next": "the **next** skill",
		"ask": "This host has no way for a tool to put a question on screen, so `decide` will say it cannot ask rather than guessing. When it does, you stop: put the one question to the user in this conversation yourself — the options in full, the reason for each, and what each costs later — and wait for them to answer. `sober decide <id> <option>` on the command line is what records what they say. Never answer on their behalf, and never treat a reply you did not get as one.",
		"spawn-guard": "**The block does not reach into this session here.** SOBER's Claude Code plugin installs a hook that denies an agent spawn onto a held node. This host gets no such hook, so SOBER can tell you the rule and cannot enforce it — which makes keeping it yours. Never spawn an agent onto a node the board holds, and never route around a decision because nothing stopped you. Say what is held and answer it instead."
	}
}
