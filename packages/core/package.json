{
	"name": "@besober/core",
	"version": "0.0.0",
	"private": true,
	"description": "SOBER's machine half: storage, the graph, derived status, git.",
	"type": "module",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"default": "./dist/index.js"
		}
	},
	"files": [
		"dist"
	],
	"engines": {
		"node": ">=22"
	},
	"scripts": {
		"build": "tsc -b",
		"typecheck": "tsc --noEmit -p tsconfig.test.json"
	},
	"dependencies": {
		"@besober/schema": "workspace:*",
		"@secretlint/secretlint-rule-preset-recommend": "^13.0.5",
		"jsonc-parser": "^3.3.1",
		"picomatch": "^4.0.7",
		"secretlint": "^13.0.5",
		"zod": "^4.5.4"
	},
	"devDependencies": {
		"@types/picomatch": "^4.0.3"
	}
}
